{{ 'custom-dev.css' | asset_url | stylesheet_tag }}

{% for block in section.blocks %}
  {{ block.settings.text }}
{% endfor %}

{% schema %}
  {
    "name": "Custom - Dev",
    "settings": [],
    "blocks": [
      {
        "type": "text",
        "name": "Text",
        "settings": [
          {
            "type": "text",
            "id": "text",
            "label": "text",
            "default": "text"
          }
        ]
      }
    ],
    "presets": [
      {
        "name": "Custom - Dev"
      }
    ]
  }
{% endschema %}